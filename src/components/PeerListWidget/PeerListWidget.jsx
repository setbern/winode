import { connect } from 'react-redux';
import React, { Component } from 'react';

class PeerListWidget extends Component {
	render() {
		return null;
	}
}

function mapStateToProps(state) {
	return {};
}

function mapDispatchToProps(dispatch) {
	return {};
}

export default connect(mapStateToProps, mapDispatchToProps)(PeerListWidget);